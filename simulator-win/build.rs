fn main() {
    //let out_dir = std::env::var("OUT_DIR").expect("Missing OUT_DIR env var.");
    //if let Err(e) = playdate_build::make_pdx_build_dir(&out_dir) {
    //    panic!("Failed to make pdx build dir in {}: {}.", out_dir, e)
    //}

    //if let Err(e) = std::fs::write(pdx_build_dir.join("pdx.bin"), ""){
        //panic!("Failed to write pdx.bin: {}.", e)
    //}
}